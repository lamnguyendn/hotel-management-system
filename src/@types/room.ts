export type Room = {
  id: number;
  floor: string;
  name: string;
  description: string;
  sort_order: number;
  is_active: boolean;
  hotel_id: number;
  room_type_id: number;
  room_status_id: number;
};
